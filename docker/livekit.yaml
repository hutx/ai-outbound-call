port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 60000
  use_external_ip: false

redis:
  address: redis:6379

keys:
  devkey: secret

logging:
  level: info

# SIP 配置
sip:
  # SIP trunk 配置通过 API 动态管理

